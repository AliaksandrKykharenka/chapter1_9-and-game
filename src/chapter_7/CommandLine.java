package chapter_7;
// withdraw all command line arguments
public class CommandLine {
    public static <test, a, is> void main(String args []) {
        for ( int  i = 0; i < args.length; i++)
        System.out.println(" args [" + i + "] : " +
                args[i]);
    }
}
